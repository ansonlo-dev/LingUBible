# Force specific platform packages to avoid SIGSEGV errors
target_platform=linux
target_arch=x64

registry=https://registry.npmjs.org/
engine-strict=true
fund=false
audit=false

# Ensure clean installations
prefer-offline=false
prefer-dedupe=true