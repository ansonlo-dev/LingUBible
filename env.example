# Appwrite 配置
APPWRITE_API_KEY=your_appwrite_api_key_here

# Resend 郵件服務配置
RESEND_API_KEY=your_resend_api_key_here

# GitHub API 配置（可選）
# 用於訪問私有倉庫或提高 API 限制
# 需要 'repo' 權限來訪問私有倉庫的 releases
VITE_GITHUB_TOKEN=your_github_personal_access_token

# 開發模式配置
VITE_DEV_MODE=true
# 設置為 true 時允許任何郵件地址註冊（僅用於開發測試）
# 生產環境請務必設置為 false

# 注意事項：
# 1. 複製此文件為 .env 並填入真實的 API 金鑰
# 2. 確保 .env 文件已添加到 .gitignore 中
# 3. 不要將真實的 API 金鑰提交到版本控制系統

# API 金鑰獲取方式：
# APPWRITE_API_KEY: Appwrite 控制台 → API Keys → 創建新金鑰
# RESEND_API_KEY: Resend 控制台 → API Keys → 創建新金鑰 (格式: re_xxxxxxxxxx)
# VITE_GITHUB_TOKEN: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
#   需要權限: repo (完整倉庫訪問權限，包括私有倉庫) 